---
title: 多環境管理
---

import Head from 'next/head'

<Head>
  <meta name="zeabur:image:title" content="多環境管理" />
  <meta name="zeabur:image:subtitle" content="為整套系統建立隔離的環境" />
</Head>

# 多環境管理

在一個專案開發中，往往需要多環境的隔離，例如正式環境、測試環境、開發環境。

Zeabur 能夠讓你輕鬆為整套系統建立多個不同的環境。

## 什麼是環境

環境就是你的服務運行的地方。它可以是一台伺服器，一個集群，一個雲端主機，甚至是一個本地機器。

不同的環境是相互獨立的。它們可以有不同的設定，不同的服務，甚至是不同版本的服務。

Zeabur 可以幫助你在一個項目中創建多個環境。

## 為什么需要多環境

在一個專案中，你可能需要將你的服務部署到不同的環境中。例如，你可能需要將你的服務部署到正式環境和開發環境中。

正式環境是為你的客戶服務的。它應該是穩定可靠的。你不應該頻繁地將服務部署到生產環境中。

開發環境是為開發者服務的。它應該是易於部署和測試的。你應該頻繁地將您的服務部署到開發環境中。

## 如何管理多環境

### 第一步：建立新環境

點擊左上角的新建環境按鈕，建立一個新的環境。

![create](/environment/create-env.png)

建立完畢後，您在目前專案中的所有服務會自動進行一次複製，在新環境中重新部署一次。

### 第二步：綁定不同分支

對於部署的程式碼，您可以在不同環境為同一個服務綁定不同的分支。

點擊服務，進入設定頁面，下拉至 **Switch Branch** ，選擇您想在該環境為其綁定的分支。

例如，當你為服務選擇了 `dev` 分支，則服務的狀態與 `dev` 分支綁定，您可以為其再起一個 `dev` 環境的域名，就能夠實時查看 `dev` 環境的線上狀態。

![create](/environment/branch.png)

